import 'package:flutter/material.dart';

class GrowGroup extends StatelessWidget {
  const GrowGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Grow Group',style: TextStyle(fontSize: 60)),
    );
  }
}
